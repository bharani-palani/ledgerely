import React from 'react';
import PropTypes from 'prop-types';

const SelfGallery = props => {
    return (<div>Hello SelfGallery</div>)
}

SelfGallery.propTypes = {
    property: PropTypes.value
};
SelfGallery.defaultProps = {};

export default SelfGallery;